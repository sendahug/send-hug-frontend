runConfig:
  vpcAccess:
    egress: ALL_TRAFFIC
    connector: api-staging-neg
    # egress: PRIVATE_RANGES_ONLY # Default value
    # networkInterfaces:
    # Specify at least one of network and/or subnetwork
    # - network: lb-network
    # subnetwork: lb-subnet
